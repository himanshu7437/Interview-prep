for(var i = 0; i < 15; i++) {
    seTimeout(function() {
        console.log(i);
    }, 1000)
} --> output

for(let  i = 0; i < 15; i++) {
    seTimeout(function() {
        console.log(i);
    }, 1000)
} --> output

is js synchronously or asynchronously?
use of async await?
shallow copy and deep copy?
event loop?
single threaded and multi threaded?
template literals?
destructuring?
higher order components?
hooks?
useRef?
useEffect?
debouncing?
implementation of redux?
